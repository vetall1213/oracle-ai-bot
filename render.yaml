services:
  - type: web
    name: oracle-ai-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: TELEGRAM_API_TOKEN
        sync: false
